spring:
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
        # properties:
      # hibernate:
      # dialect: org.hibernate.dialect.MySQLDialect
    show_sql: false
springdoc:
  paths-to-match: /api/**/v1/**
  swagger-ui:
    use-root-path: true
server:
  port: 8888